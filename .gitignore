node_modules/
build/

# Deployment archives produced by `npm run package`
dist/

# Local environment overrides (.env.production is committed — it holds build
# flags, not secrets)
.env.local
.env.*.local

# Editor / OS noise
.DS_Store
Thumbs.db
*.log
npm-debug.log*
